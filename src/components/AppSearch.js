import React from 'react';
import { View, StyleSheet } from 'react-native';

const AppSearch = () => {

    return (
        <View style={styles.appSearchContainer}></View>
    )
}

const styles = StyleSheet.create({
    appSearchContainer: {
        flex: 1
    }
})

export default AppSearch;